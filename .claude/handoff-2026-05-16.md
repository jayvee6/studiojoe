# Studio Joe v1 rebuild — handoff (2026-05-16)

Plan: `~/.claude/plans/i-think-the-showcase-logical-penguin.md` (Studio Joe v1 — Showroom + Sandbox; 4-zone studio; "crack it open"; zone-atmosphere).

Branch: `staging` (off `main`; direct push to `main` blocked → PR at S4). Vercel auto-previews `staging`. Zero-build static; `node serve.js` :3000; CI on PR→main = `npx htmlhint "*.html"` + lychee link-check.

Preserve untouched: `resume.html`, `ipod.html`, `api/apple-token.js`, `musicplayer/`.

## Log (one line per chunk)

- 2026-05-16 S0 — staging branch; vendored sj-design `lib/{canvas-size,synthetic-pulse,envelopes,webgpu-bootstrap}.js`; rebuilt `index.html` as 4-zone studio shell (Showroom + Sandbox real, Shelf real-links, Workbench honest "coming"); retired Blue Hour indigo → new "Nocturne/Aurora" palette (graphite base, electric-teal accent #2DD4BF) on the same 13-token contract; carried header/nav/footer/theme-toggle/localStorage/grain/starfield/Perlin verbatim (deco emojis scoped to #showroom); htmlhint clean (3 files, 0 errors). → commit + push staging

- 2026-05-16 S1 — Showroom WebGPU hero: `#hero-gpu` canvas absolute in #showroom (z0), `.hero-scrim` (z1), `.showroom-inner` content (z2); `#showroom::before` atmosphere gradient = silent fallback (no WebGPU → quiet return, never blank, no error overlay); verbatim wire-terrain WGSL (56-step/4-bisection, renderScale 0.66, maxDPR 1); SJPulse via `/lib/synthetic-pulse.js` + inline fallback; rAF pauses when scrolled past Showroom. htmlhint clean; inline JS `node --check` OK. → `7426375`
- 2026-05-16 S2 — `7426375..f1aef57` "crack it open": registry-driven generic component (`[data-crack]` chip + `[data-crack-open]` trigger), glass slide-in panel (what → real source → Take it/Copy), a11y (focus trap/Esc/restore), `window.CrackOpen` exposed. Wired hero + sj-design. htmlhint+JS OK.
- 2026-05-16 S3 — `35a4fd0` Sandbox: emoji-rain + N-gon morph on one DPR-fit Canvas2D, pointer-driven, SJPulse/VizEnv from /lib/, off-screen pause, reuses crack-it-open (`sandbox` REG). htmlhint+JS OK.
- 2026-05-16 S4a — `4c74fca` zone-atmosphere: per-zone --accent/--atmo, `#atmo` glow layer, IntersectionObserver sets body[data-zone], CSS crossfade. htmlhint+JS OK.
- 2026-05-16 S4b — Workbench "how the machine ships" pipeline diagram (Canvas node-loop, zone-accent-aware, off-screen pause, reduced-motion static), `machine` REG entry, honest "blog/video — coming". htmlhint clean, JS OK, all link targets resolve. → commit + push staging + PR staging→main

- 2026-05-16 fix — link-check caught github.com/jayvee6/sj-design 404 (private repo); repointed Shelf card + 3 crack "Take it" targets → github.com/jayvee6, copy made truthful. `30fe9a4`. **PR #8 staging→main: ALL CI GREEN** (HTML ✓ / Link ✓ / Vercel ✓). v1 complete; awaiting user merge → Vercel promotes to studiojoe.dev.

- 2026-05-16 fix(layout) — `ad2487c` Sandbox formation rendered outside its stage box. Root cause (all 3 canvases): backing store sized synchronously at parse time before flex+aspect-ratio resolved; hero sized to window not its taller #showroom box. Fix: shared `fitObserve(el,cb)` rAF-debounced ResizeObserver re-fits canvas to the element's real box. PR #8 still green.

## Next (post-merge / v2)
- S2 — "crack it open" mechanic (prototype first).
- S3 — Sandbox interactive (emoji-rain + manipulable viz), each with crack-it-open.
- S4 — zone-atmosphere system + Shelf/Workbench polish + one diagram + PR staging→main.

Sources staged in context: full wire-terrain WGSL+loop, all 4 lib APIs, sj-design formations/scenes manifest, emoji-visualizations.md (powers crack-it-open).
- 2026-05-16 fix(layout2) — `8c19a9e` Safari aspect-ratio+flex-stretch bug made stage oversized → diagram overflowed. .zone align-items:flex-start; .sandbox-stage definite box (no aspect-ratio); canvas 100%/100%; .shelf-grid own width; nodePos caps rx/ry for label padding. CI green.
- 2026-05-16 fix(theme) — `74c0173` removed light-mode toggle; site is dark-only. Light palette put dark text over the always-dark WebGPU hero → unreadable headline. Dropped `[data-theme="light"]` palette + 4 light zone-accent overrides + light header/showroom::before/hero-scrim/footer rules + `#theme-btn` chrome + nav button + localStorage theme JS. 13-token contract + zone-atmosphere accents unchanged; htmlhint clean, inline JS `node --check` OK, no residual `data-theme`. **PR #8 ALL CI GREEN** (HTML ✓ 11s / Link ✓ 4s / Vercel ✓ deployed). Awaiting user eyeball + merge.
- 2026-05-16 merge — PR #8 staging→main merged (`770ff58`, merge commit — preserves S0–S4b SHAs the log references; staging branch kept as working branch). Vercel Production deploy `state=success` on `770ff58` → **studiojoe.dev live** with Studio Joe v1, dark-only.
- 2026-05-16 feat(workbench W1) — `01a610b` built out the scaffolded Workbench into the diagram-forward "how the work gets made" co-engineering showcase. Sharpened eyebrow/h2/lede (co-engineering with Claude under the defensive-engineering playbook). Added `.mx-grid` of 4 pillar cards — **Two auditors/one arbiter**, **Context is managed state**, **A fleet of specialists**, **Fast/loud failure** — each a spec-driven static schematic (one generic renderer: nodes+edges normalized coords → fitObserve-scaled, zone-accent re-tint via MutationObserver on body[data-zone], reduced-motion-correct, parity with shipped Sandbox/Machine canvases) + crack-it-open to the REAL contract distilled from `~/.claude` → dev-playbook/best_practices.md (§ One Arbiter Two Auditors / Context & State / Orchestrator rules / Error taxonomy) and the REAL ~/.claude skill fleet (graphics-api-expert + bug-hunt-swarm/review-swarm/orchestrate-batch-refactor). Zero-Guessing: grounded via Explore inventory, nothing invented. Verified: htmlhint clean (3/0), inline JS `node --check` OK, DOM correct, layout at 1440 = grid 1100 / cards 352 (like .shelf-grid), crack wiring 4↔4↔4. Canvas `4×…` in headless preview is a known rAF-throttle artifact — shipped-working Sandbox/Machine canvases show the identical symptom there → harness, not code; real-browser eyeball via Vercel preview as per every prior chunk. **Next: W2** — animate the 4 schematics (rAF + pulse + off-screen pause; reduced-motion keeps W1 static), polish, PR staging→main. Then (separate later chunk, per user) the resume.html professional section.
- 2026-05-16 feat(resume R1) — `2e52737` redo of resume.html, chunk 1: ported to the dark-only Nocturne/Aurora system (matches studiojoe.dev — graphite base, aurora `--bg-g`, electric-teal accent, shared 13-token contract). Retired Blue Hour indigo + the ENTIRE `[data-theme="light"]` system (palette + header/.btn-ghost/.e-card/.now-card/.card light overrides), `#theme-btn` chrome + nav button + localStorage toggle JS; `.btn-solid` glow indigo→teal, dark-on-teal text. Content/atmosphere/font-sizer untouched (font-sizer auto-matches via shared tokens). Verified: no residual data-theme/Blue-Hour refs, htmlhint 3/0, inline JS `node --check` OK. −80 net lines.
- 2026-05-16 feat(resume R2) — `8c11229` redo chunk 2: blend positioning + co-engineering section. Hero keeps real title + the exact 10+yr SQE bio sentences verbatim (recruiter anchor) + one truthful clause (off-the-clock real-time-graphics tools, multi-agent Claude workflow). New `#method` "Co-engineering with AI" section (eyebrow + heading + 3 now-cards, established pattern): Two auditors/one arbiter · Context as managed state · A fleet of specialists — distilled resume-toned from the SAME real ~/.claude playbook + skill fleet as the Workbench, nothing invented. Nav +Co-engineering link; `#method` added to z-index:12 set (above starfield) + deco SECTIONS method pool. htmlhint 3/0, inline JS OK. User chose: push staging + open PR→main (W1+R1+R2 ride together). R3 polish (real-browser eyeball / reduced-motion) deferred to post-eyeball. W2 (animate Workbench schematics) still queued.
- 2026-05-16 ship — PR #9 staging→main merged (`8eb0d37`); Vercel Production `state=success` → studiojoe.dev live with W1 Workbench showcase + R1/R2 resume redo.
- 2026-05-16 feat W2+R3 (parallel agents) — user: "both spread over multiple agents". Orchestrated 2 concurrent general-purpose agents (clean fan-out: W2=index.html only, R3=resume.html only, no overlap; agents did NO git; I = Pedant/Critic/Arbiter fan-in). **W2 `e7f37ff`** animate the 4 Workbench pillar schematics: one shared rAF, slow ~4.5s/edge accent pulse (Machine() radial-gradient idiom + SJPulse.wave breath), per-diagram phase stagger, off-screen pause (whole-row `every(offscreen)` + per-card skip); `prefers-reduced-motion` keeps the byte-identical static draw (`phase==null` ⇒ `(key||lit)≡key`), rAF gated behind `!REDUCED`; SPEC/4×register()/markup/CSS untouched. **R3 `e9b25c9`** resume.html reduced-motion a11y: single `REDUCED` matchMedia gate — `[data-sr]` shown final-state (no reveal), deco emojis static at base opacity (ticker drift + scrub skipped), starfield static (no fade/twinkle), `@media (prefers-reduced-motion:reduce){html{scroll-behavior:auto}}` added; non-reduced path byte-for-byte unchanged; ZERO copy/content changes (diff-verified). Independent verify: htmlhint 3/0, both inline scripts `node --check` OK, diffs reviewed. Shipping: commit 2 chunks + handoff → push staging → PR #10 staging→main.
- 2026-05-16 ship — PR #10 staging→main merged (`3901c18`); Vercel Production `state=success` → studiojoe.dev live with W2 animated Workbench schematics + R3 resume reduced-motion a11y. W/R track complete.
- 2026-05-16 feat(site) — `5be4a0c` user supplied public+live `jayvee6.github.io/sj-audio-bridge` ("Native macOS menubar helper: ScreenCaptureKit system audio → localhost WebSocket"). Verified both URLs 200 BEFORE editing (lychee lesson). Chose (user-picked) "Shelf card + hero crack link": new Shelf "Tool" card → Pages URL (next to sj-design); wire-terrain crack-it-open copy now names SJ Audio Bridge as the real audio-swap, `take` retargeted generic profile → `github.com/jayvee6/sj-audio-bridge` (matches "Take it → GitHub" label). htmlhint 3/0, inline JS node --check OK. Next: push staging → PR staging→main → user merge.
